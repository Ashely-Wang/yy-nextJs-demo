内网环境：
1.工作组：默认模式，平等，但不利于管理
2.域：集中/统一管理
3.域的组成：(1)域控制器DC(2)成员机
活动目录(AD)(整个域的最核心，也可以理解为核心数据库)，域帐号，组策略GPO(group policy)
域之所以可以实现集中管理，全靠活动目录
安装了域控制器，就生成了域
安装了活动目录就剩成了域控制器

本地管理员组：administrators
域管理员组：Domain Admins
组的目的是为了赋权限，OU是为了下发组策略
本地组策略的优先级最低
当上级强制和下级组织继承时，强制生效